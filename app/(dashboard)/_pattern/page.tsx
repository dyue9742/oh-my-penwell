import { PaperMainContentStyle } from "@/app/_lib/utils/paper_color_style";
import SignedLayout from "@/app/(dashboard)/layout";

const Pattern = () => {
  return (
    <SignedLayout>
      <div style={PaperMainContentStyle}>fn Pattern; todo!();</div>
    </SignedLayout>
  );
};

export default Pattern;
